spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/learningdb
    username: root
    password: root
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
