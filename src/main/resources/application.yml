spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/learningdb
    username: root
    password: root
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
